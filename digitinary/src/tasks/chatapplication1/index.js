// src/tasks/chatapplication1/index.js
import React from "react";
import App from "./App";

const ChatApplication = () => {
  return (
    <div>
      <App /> {/* Render the ChatApplication app */}
    </div>
  );
};

export default ChatApplication; // Exporting the component as default
